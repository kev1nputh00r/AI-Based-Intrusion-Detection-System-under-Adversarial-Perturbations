# AI-Based-Intrusion-Detection-System-under-Adversarial-Perturbations
This project implements a hybrid IDS that combines supervised classification and anomaly detection. It supports configurable preprocessing, calibration, ablation studies, and adversarial evaluation (noise, feature masking, FGSM-style perturbations). Outputs include metrics, ROC curves, and saved models.
